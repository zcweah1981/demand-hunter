import {RunDailyButton, AutoTickButton, ExportReportButton} from './Actions'
export function RunStatus({auto}:{auto:any}){
 const last=auto?.last_run; const s=last?.summary||{}; const running=last?.status==='running'
 const pct=running&&s.total?Math.round((s.current||0)*100/s.total):last?.status==='ok'?100:0
 return <section className="panel space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-bold">自动找词流程</h2><p className="mt-1 text-sm text-slate-400">Root Library → Keywords → SERP → Scoring → Cards → Review Feedback</p></div><div className="flex flex-wrap gap-2"><RunDailyButton/><AutoTickButton/><ExportReportButton/></div></div><div className="grid gap-4 md:grid-cols-4"><div className="card"><div className="kpi-label">Auto</div><b className={auto.enabled?'text-emerald-300':'text-rose-300'}>{auto.enabled?'Enabled':'Disabled'}</b></div><div className="card"><div className="kpi-label">Interval</div><b>{auto.interval_minutes} min</b></div><div className="card"><div className="kpi-label">Last Run</div><b>{last?`#${last.id} ${last.status}`:'none'}</b></div><div className="card"><div className="kpi-label">Progress</div><b>{pct}%</b></div></div>{last&&<div className="rounded-2xl border border-slate-700 bg-slate-950 p-4"><div className="mb-2 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-blue-500" style={{width:`${pct}%`}} /></div><pre className="whitespace-pre-wrap text-xs text-slate-300">{JSON.stringify(last.summary,null,2)}</pre></div>}</section>
}
