import Link from 'next/link'
import {LogoutButton} from './LogoutButton'
const items=[['/','Dashboard'],['/review','Review Queue'],['/cards','Cards'],['/keywords','Keywords'],['/roots','Root Library'],['/runs','Run History'],['/settings','Settings']]
export function Nav(){return <aside className="sticky top-0 min-h-screen w-72 border-r border-slate-800 bg-slate-950/90 p-5 backdrop-blur"><div className="mb-8"><div className="text-2xl font-black text-white">Demand Hunter</div><div className="mt-1 text-xs uppercase tracking-[0.25em] text-blue-300">Opportunity OS</div></div><nav className="space-y-2">{items.map(([href,label])=><Link className="block rounded-xl px-3 py-2 text-slate-300 hover:bg-slate-800 hover:text-white" href={href} key={href}>{label}</Link>)}</nav><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/70 p-4 text-xs text-slate-400"><div className="mb-2 font-semibold text-slate-200">Quality Formula</div><p>需求 × 入口 × 缺口 × MVP × 变现</p></div><LogoutButton/></aside>}
