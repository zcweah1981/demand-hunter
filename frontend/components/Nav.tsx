'use client'
import Link from 'next/link'
import {LogoutButton} from './LogoutButton'
import {useLang} from '../lib/i18n'
const items=[['/overview','overview'],['/discovery','discovery'],['/','dashboard'],['/review','review'],['/cards','cards'],['/keywords','keywords'],['/roots','roots'],['/runs','runs'],['/settings','settings']]
export function Nav(){const {lang,setLang,t}=useLang(); return <aside className="sticky top-0 min-h-screen w-72 border-r border-slate-800 bg-slate-950/90 p-5 backdrop-blur"><div className="mb-8"><div className="text-2xl font-black text-white">Demand Hunter</div><div className="mt-1 text-xs uppercase tracking-[0.25em] text-blue-300">Opportunity OS</div><div className="mt-4 inline-flex rounded-xl border border-slate-700 bg-slate-900 p-1"><button className={`rounded-lg px-3 py-1 text-xs ${lang==='zh'?'bg-blue-600 text-white':'text-slate-400'}`} onClick={()=>setLang('zh')}>中文</button><button className={`rounded-lg px-3 py-1 text-xs ${lang==='en'?'bg-blue-600 text-white':'text-slate-400'}`} onClick={()=>setLang('en')}>EN</button></div></div><nav className="space-y-2">{items.map(([href,key])=><Link className="block rounded-xl px-3 py-2 text-slate-300 hover:bg-slate-800 hover:text-white" href={href} key={href}>{t(key)}</Link>)}</nav><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/70 p-4 text-xs text-slate-400"><div className="mb-2 font-semibold text-slate-200">{t('qualityFormula')}</div><p>{t('formula')}</p></div><LogoutButton/></aside>}
