import {api} from '../../lib/api'; import {I18nText} from '../../components/I18nText'
export default async function Page(){const rows=await api<any[]>('/api/roots'); const groups=[...new Set(rows.map(r=>r.category))]; return <div className="space-y-6"><div><h1 className="text-3xl font-bold"><I18nText zh='词根库' en='Root Library'/></h1><p className="mt-2 text-slate-400"><I18nText zh='自动找词的种子库。反馈会调整 weight；Block 会禁用 root。' en='Seed library for automatic keyword discovery. Feedback adjusts weights; Block disables roots.'/></p></div>{groups.map(g=><section className="panel" key={g}><div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-bold">{g}</h2><span className="badge">{rows.filter(r=>r.category===g).length}</span></div><div className="grid gap-3 md:grid-cols-3 xl:grid-cols-4">{rows.filter(r=>r.category===g).map(r=><div key={r.id} className="rounded-2xl border border-slate-800 bg-slate-950/70 p-4"><div className="flex justify-between"><b>{r.term}</b><span className={r.enabled?'badge badge-action':'badge badge-reject'}>{r.enabled?'on':'off'}</span></div><p className="mt-2 text-sm text-slate-400">weight {r.weight}</p>{r.notes&&<p className="mt-1 text-xs text-slate-500">{r.notes}</p>}</div>)}</div></section>)}</div>}
