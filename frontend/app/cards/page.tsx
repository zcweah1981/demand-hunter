import {api, Card} from '../../lib/api'; import {OpportunityCardView} from '../../components/OpportunityCard'
export default async function Page(){const rows=await api<Card[]>('/api/cards'); const groups=['Action','Watch','Reject']; return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Opportunity Cards</h1><p className="mt-2 text-slate-400">按 verdict 分组查看。Action 才进入执行候选；Watch 保留观察；Reject/Block 用来训练系统。</p></div>{groups.map(g=>{const list=rows.filter(r=>r.verdict===g);return <section className="panel" key={g}><div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-bold">{g}</h2><span className="badge">{list.length}</span></div><div className="grid gap-4 lg:grid-cols-2">{list.length?list.map(c=><OpportunityCardView key={c.id} card={c}/>):<p className="text-sm text-slate-500">No {g} cards.</p>}</div></section>})}</div>}
