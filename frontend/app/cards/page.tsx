import {api, Card} from '../../lib/api'; import {Feedback} from '../../components/Actions'
export default async function Page(){const rows=await api<Card[]>('/api/cards'); return <div className="space-y-6"><h1 className="text-3xl font-bold">Opportunity Cards</h1>{rows.map(c=><article className="card" key={c.id}><div className="flex justify-between"><h2 className="text-xl font-semibold">{c.title}</h2><span className="badge">{c.verdict} · {c.score}</span></div><div className="mt-3 grid grid-cols-5 gap-2 text-sm text-slate-300">{[['Demand',c.demand_score],['SERP Gap',c.serp_gap_score],['Weak Competitor',c.competitor_weakness_score],['MVP',c.mvp_score],['Money',c.monetization_score]].map(([k,v])=><div className="rounded bg-slate-950 p-2" key={k}>{k}<br/><b>{v}</b></div>)}</div><p className="mt-4">{c.mvp_plan}</p><p className="mt-2 text-sm text-green-300">{c.monetization_type}</p>{c.risks?.length>0&&<ul className="mt-2 list-disc pl-5 text-sm text-amber-300">{c.risks.map((r:string)=><li key={r}>{r}</li>)}</ul>}<div className="mt-4"><Feedback id={c.id}/></div></article>)}</div>}
