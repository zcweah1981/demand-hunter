import {api, Card} from '../../lib/api'; import {OpportunityCardView} from '../../components/OpportunityCard'; import {I18nText} from '../../components/I18nText'
export default async function Page(){const rows=(await api<Card[]>('/api/cards')).filter(c=>!c.feedback_label); return <div className="space-y-6"><div><h1 className="text-3xl font-bold"><I18nText zh='复核队列' en='Review Queue'/></h1><p className="mt-2 text-slate-400"><I18nText zh='这是训练入口。不要放过垃圾卡：Reject/Block 会影响下一轮找词。' en='This is the training entry. Reject/Block bad cards so the next discovery loop learns.'/></p></div><div className="grid gap-4 lg:grid-cols-2">{rows.length?rows.map(c=><OpportunityCardView card={c} key={c.id}/>):<div className="panel text-slate-400"><I18nText zh='暂无待复核卡片。' en='No cards pending review.'/></div>}</div></div>}
